# E2E Report — gnom-hub-v1 ↔ Tollgate

**Date:** 2026-08-13  
**Workspace:** `TOLLGATE_HOME=/Users/landjunge/WS-gnom-hub-v1`  
**Stack:** Gnom `:8080` · Tollgate `:8787` (HTTP proxy mode)  
**Wiring:** `GNOM_TOLLGATE_LLM=1` · `TOLLGATE_URL=http://127.0.0.1:8787` · `TOLLGATE_CONSUMER=gnom`

## Command

```bash
export TOLLGATE_HOME=/Users/landjunge/WS-gnom-hub-v1
export GNOM_URL="${GNOM_URL:-http://127.0.0.1:${GNOM_DESK_PORT:-}}"  # Gnom-Hub-V1 desk, not Tollgate :8787
./scripts/e2e-gnom-hub.sh all
```

## Results — **PASS 13 / fail 0**

| # | Test | Result | Notes |
|---|------|--------|-------|
| T1 | Normal chat `/v1/chat/completions` | **PASS** | choices returned for consumer `gnom` |
| T2 | Budget / protect deny | **PASS** | BLOCKED path on tight envelope |
| T4 | Agent loop `tool_calls_est` | **PASS** | max_tool_calls hard-stop |
| T3 | Chaos failover (`deepseek` inject) | **PASS** | routed to `opencode_zen`; chat after chaos OK |
| — | Control plane | **PASS** | snapshot mentions `gnom` |
| — | Gnom `/api/chat` | **PASS** | job done (brainstorm) |
| T5 | Restart recovery | **PASS** | kill → restart → chat OK; `keys_app.json` intact |

## Unit regression

```text
pytest → 181 passed
```

## Findings

| # | Observed | Side | Keep / fix later |
|---|----------|------|------------------|
| 1 | Stream with model `deepseek-chat` can error mid-SSE if upstream model id unsupported; non-stream routes to free model | Route | Prefer `tollgate/free` or routed model ids in clients |
| 2 | Gnom brainstorm notes can accumulate session history after long runs | Gnom | Reset between demos (`/api/reset`) |
| 3 | Modular dual-path: stream + gateway share `protect.package_deny` | Tollgate | Keep |

## Product question

> Would I leave Tollgate running in front of gnom-hub tomorrow?

**Yes** — Protect/Route/Prove exercised; consumer `gnom` metered; restart-safe data under `WS-gnom-hub-v1`.

## Related

- Plan: `docs/TEAM_PLAN_MODULAR_REWORK.md` (Phases 0–7 + E2E closed)  
- Protocol: `docs/E2E_GNOM_HUB.md`
