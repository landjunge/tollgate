# n8n → Tollgate (OpenAI-compatible credentials)
# In n8n: Credentials → OpenAI API
OPENAI_API_BASE=http://127.0.0.1:8787/v1
# open mode: any label; with auth: n8n:<secret from tollgate consumer-add>
OPENAI_API_KEY=n8n
# preferred free route model id
N8N_MODEL=tollgate/free
